{
  "name": "kimux-blockchain-integration",
  "version": "1.0.0",
  "description": "KimuX smart contracts for wallet, commission, and escrow flows",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy-all.js --network localhost",
    "deploy:hardhat": "hardhat run scripts/deploy-all.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "solc": "^0.8.26"
  }
}
